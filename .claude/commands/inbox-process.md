---
description: Inboxとmemos.txtを整理し、各フォルダへ振り分ける
argument-hint: [優先処理するInboxノートのファイル名(省略時は全件)]
---

`00_Inbox/` の未整理ノートと `../memos.txt` の未取り込みメモを整理してください。対象指定: $ARGUMENTS

**引数でファイル名が指定されている場合(優先処理モード)**: memos.txt の取り込み(手順1〜2)はスキップし、`00_Inbox/` 内の指定ノート**1件だけ**を手順3の要領で振り分けて、報告・コミット・プッシュしてください。他のノートには触れないこと。

## 手順

1. `90_System/memos-imported.md` を読み、取り込み済みの memos.txt 項目番号を確認する(ファイルがなければ全項目が未取り込み)
2. `../memos.txt` の未取り込み項目を、それぞれ1件ずつ `00_Inbox/` にMarkdownノートとして保存する(memos.txt 自体は変更しない)。取り込んだ項目番号を `90_System/memos-imported.md` に追記する
   - `../memos.txt` が存在しない環境(GitHub Actions上での実行)では、この手順をスキップして `00_Inbox/` の処理のみ行う
3. `00_Inbox/` の各ノートについて:
   - frontmatter(`created`, `tags`, `status`)を付与・補完する
   - 内容を読んで分類し、適切なフォルダへ**移動**する
     - 日々の出来事・感想 → `10_Journal/`(該当日のファイルに統合)
     - 進行中の取り組みに関するもの → `20_Projects/<プロジェクト名>/`
     - 技術的な知識・手順 → `30_Tech_Notes/`
     - 投資・ビジネス・ドメイン知識 → `50_Business_Context/`
   - 分類に迷っても保留せず、最も妥当と判断した先へ移動する。ノート末尾に「分類判断: <採用した案と根拠、検討した他の案>」を追記する。ユーザーへの質問で終わらせない(ヘッドレス実行では回答できない)
4. 処理結果(取り込み件数・振り分け先・保留件数)を報告する
5. 変更をコミットする(例: `inbox: N件整理(YYYY-MM-DD)`)

## ガードレール

- 1回の実行で処理するのは最大10件。超過分は次回に回し、その旨を報告する
- ファイルの削除は禁止。統合した場合も元ノートは `99_Archives/` へ移動する
